{
  "name": "Python Development Environment",
  "image": "mcr.microsoft.com/devcontainers/base:alpine",
  "postCreateCommand": "apk add --no-cache python3 py3-pip && pip install --upgrade pip && pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  },
  "forwardPorts": [8888]
}
